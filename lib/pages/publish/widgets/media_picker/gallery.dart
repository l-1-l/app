import 'package:flutter/material.dart';

class MediaPickerGallery extends StatelessWidget {
  const MediaPickerGallery({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
